# Optic Backend

## ENV configuration
Create a .env file on the root folder with this arguments:
```cmd
    SERVER_ADDR=
    PG.USER=
    PG.PASSWORD=
    PG.HOST=
    PG.PORT=
    PG.DBNAME=
    PG.POOL.MAX_SIZE=
```